# Compaction note

During future compaction or handoff rollouts, include [000_god_prompt.md](/home/willi/coding/trading/soldem-2026/v2/research_logs/000_god_prompt.md) as a required reference for the original specification and constraints.

This repository now contains:

- Rules/profile system in `game/rules.py`.
- Rarity-based ranking policy and alternatives in `game/utils.py`.
- Strategy framework in `strategies/`.
- Simulation and tournament tooling in `sim/` and `scripts/`.
- Live advisor API in `game/api.py` and `game/advisor.py`.
- Day-of operating docs in `docs/day_of_runbook.md` and `docs/day_of_patch_guide.md`.
